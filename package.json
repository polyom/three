{
  "name": "three",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
		"ci": "yarn install --immutable --immutable-cache --check-cache",
		"clean": "rm -rf docs"
  },
  "devDependencies": {
    "@types/node": "^17.0.16",
    "@types/three": "^0.137.0",
    "typescript": "^4.5.4",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "three": "^0.137.5"
  }
}
